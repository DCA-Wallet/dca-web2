import {Button as ButtonMUI, styled} from "@mui/material";

export const Button = styled(ButtonMUI)`
  display: inline-flex;
  justify-content: flex-end;
  align-items: center;
  border-radius: 16px;
  background: linear-gradient(210deg, #C3B1F3 0%, #F1ABDB 100%);
  color: white;
  
  text-transform: none;

  font-size: 21px;
  font-style: normal;
  font-weight: 500;
  line-height: 28px;
`
